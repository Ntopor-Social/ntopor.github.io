// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  // output: "export",
  // basePath: '/ntopor.github.io',
  /* config options here */
};

module.exports = nextConfig;
